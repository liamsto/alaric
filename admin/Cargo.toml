[package]
name = "aadmin"
version = "0.1.0"
edition.workspace = true

[lints]
workspace = true

[dependencies]
alaric-lib = { path = "../lib" }
tokio = { version = "1.50.0", features = ["rt-multi-thread", "macros"] }
